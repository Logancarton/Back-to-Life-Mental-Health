const header=document.querySelector('[data-header]');const menuToggle=document.querySelector('[data-menu-toggle]');const nav=document.querySelector('[data-nav]');const updateHeader=()=>{if(!header)return;header.classList.toggle('scrolled',window.scrollY>8)};updateHeader();window.addEventListener('scroll',updateHeader,{passive:true});if(menuToggle&&nav){menuToggle.addEventListener('click',()=>{const isOpen=nav.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(isOpen));document.body.classList.toggle('menu-open',isOpen)});nav.querySelectorAll('a').forEach(link=>{link.addEventListener('click',()=>{nav.classList.remove('open');menuToggle.setAttribute('aria-expanded','false');document.body.classList.remove('menu-open')})})}document.querySelectorAll('[data-year]').forEach(node=>{node.textContent=new Date().getFullYear()});document.querySelectorAll('a[href="services.html#medication-management"],a[href="#medication-management"]').forEach(link=>{link.setAttribute('href','medication-management.html')});const conditionPages=new Map([['Anxiety','anxiety.html'],['Depression','depression.html'],['ADHD','adhd.html'],['PTSD','ptsd.html'],['OCD','ocd.html'],['Bipolar Disorder','bipolar.html'],['Grief & Loss','grief-loss.html'],['Life Transitions','life-transitions.html']]);document.querySelectorAll('.condition-card').forEach(card=>{const label=card.querySelector('strong')?.textContent.trim();const href=conditionPages.get(label);if(href)card.setAttribute('href',href)});document.querySelectorAll('.site-footer [href="contact.html"]').forEach(contactLink=>{const section=contactLink.closest('div');if(!section||section.querySelector('[href="privacy.html"]'))return;const privacyLink=document.createElement('a');privacyLink.href='privacy.html';privacyLink.textContent='Privacy Policy';section.appendChild(privacyLink)});const revealItems=document.querySelectorAll('.reveal');if('IntersectionObserver'in window&&!window.matchMedia('(prefers-reduced-motion: reduce)').matches){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.12});revealItems.forEach(item=>observer.observe(item))}else{revealItems.forEach(item=>item.classList.add('visible'))}